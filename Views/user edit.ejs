<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Edit</title>
    <link rel="stylesheet" href="/css/userprofile.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Finger+Paint&family=Kanit:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto+Slab:wght@100..900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=K2D:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800&display=swap"
        rel="stylesheet">
</head>

<body>
    <main>
        <div class="edit-container">
            <h1>Edit Info</h1>
            <form action="/save/<%= record.id %>" method="post">
                <div class="whole-form">
                    <div class="container">
                        <div class="name-input">
                            <label for="name">Name:</label>
                            <input type="text" name="name" value="<%= record.name %>">
                        </div>

                        <div class="email-input">
                            <label for="email">Email:</label>
                            <input type="text" name="email" value="<%= record.email %>">
                        </div>

                        <div class="gender-input">
                            <label for="gender">Gender:</label>
                            <select name="gender" id="gender">
                                <option value="male" <%=record.gender==="male" ? "selected" : "" %>>Male</option>
                                <option value="female" <%=record.gender==="female" ? "selected" : "" %>>Female</option>
                            </select>
                        </div>

                        <div class="password-input">
                            <label for="password">Password:</label>
                            <input type="text" name="password" value="<%= record.password %>">
                        </div>
                    </div>
                </div>

                <button class="save-btn" type="submit">Save Edit</button>
            </form>
    </main>

    </div>


    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="/js/script.js"></script>
</body>

</html>